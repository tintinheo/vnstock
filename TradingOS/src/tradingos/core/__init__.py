"""
core/ — Domain logic: signal generation, strategy rules, risk management.

Modules:
    signals.py      — Buy/sell signal classification and scoring
    strategy.py     — Strategy definitions and entry/exit rules
    risk.py         — Position sizing, Kelly criterion, stop-loss rules
    calendar.py     — VN trading calendar, T+2 settlement, holidays
"""
